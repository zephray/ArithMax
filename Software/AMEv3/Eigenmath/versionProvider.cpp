#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "versionProvider.hpp"
void getVersion(char* buffer) {
  strcpy(buffer, "XX");
}
void getTimestamp(char* buffer) {
  strcpy(buffer, "2013-10-26");
}